import open from "open";
import path from "path";

export default function(task_number) {
  if (task_number == 1) {
    console.log("Нет задач");
  }
  if (task_number == 2) {
    console.log("Нет задач");
  }
};
